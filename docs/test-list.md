# ToDo API テスト項目一覧

## 1. 機能テスト（正常系）
### 1.1 GET /todos エンドポイント
[X] 全ToDoアイテムが正常に取得できること
[X] ToDoが存在しない場合は空の配列が返ること
[X] statusパラメータで絞り込みが正常に機能すること
[X] not_startedのToDoのみが返ること
[X] in_progressのToDoのみが返ること
[X] completedのToDoのみが返ること
[X] priorityパラメータで絞り込みが正常に機能すること
[X] lowのToDoのみが返ること
[X] mediumのToDoのみが返ること
[X] highのToDoのみが返ること
[X] due_beforeパラメータで期限日の絞り込みが正常に機能すること
[X] due_afterパラメータで期限日の絞り込みが正常に機能すること
[X] 複数のクエリパラメータを組み合わせた絞り込みが正常に機能すること

### 1.2 POST /todos エンドポイント
[X] 必須項目（title）のみで新規ToDoが作成できること
[X] 全フィールド指定で新規ToDoが作成できること
[X] created_atとupdated_atが自動的に設定されること
[X] priorityのデフォルト値（medium）が正しく設定されること
[X] statusのデフォルト値（not_started）が正しく設定されること
[X] 作成後のレスポンスで正しくToDoの情報が返ること
[X] idが自動採番されること

### 1.3 GET /todos/{id} エンドポイント
[X] 存在するidのToDoが正常に取得できること
[X] レスポンスに全フィールドが含まれていること

### 1.4 PUT /todos/{id} エンドポイント
[X] 単一フィールドのみの更新が正常に行われること
[X] 複数フィールドの更新が正常に行われること
[X] 更新されていないフィールドは元の値が維持されること
[X] updated_atが更新時に自動的に更新されること
[X] created_atが更新時に変更されないこと
[X] 更新後のToDoが正しく返ること
[X] 同じToDoの現在のタイトルでの更新が正常に処理されること

### 1.5 DELETE /todos/{id} エンドポイント
[X] 存在するToDoが正常に削除できること
[X] 削除後に204のステータスコードが返ること
[X] 削除済みのToDoが取得操作で404を返すこと
[X] 同じIDの削除を複数回行った場合、最初は成功し、2回目以降は404エラーが返すこと


## 2. バリデーションテスト（異常系）
### 2.1 共通
[X] パスパラメータのidが数値でない場合に400エラーが返ること
[X] パスパラメータのidが負数または0の場合に400エラーが返ること
[X] 存在しないidの場合に404エラーが返ること

### 2.2 GET /todos クエリパラメータ
[X] statusに無効な値を指定した場合に400エラーが返ること
[X] priorityに無効な値を指定した場合に400エラーが返ること
[X] due_beforeまたはdue_afterに無効な日付形式を指定した場合に400エラーが返ること

### 2.3 POST /todos バリデーション
[X] titleが省略された場合に400エラーが返ること
[X] titleが空文字列の場合に400エラーが返ること
[X] titleが100文字を超える場合に400エラーが返ること
[X] descriptionが500文字を超える場合に400エラーが返ること
[X] priorityに無効な値を指定した場合に400エラーが返ること
[X] statusに無効な値を指定した場合に400エラーが返ること
[X] due_dateに無効な日付形式を指定した場合に400エラーが返ること
[X] 同一タイトルのToDoがすでに存在する場合に409エラーが返ること

### 2.4 PUT /todos/{id} バリデーション
[X] titleが空文字列の場合に400エラーが返ること
[X] titleが100文字を超える場合に400エラーが返ること
[X] descriptionが500文字を超える場合に400エラーが返ること
[X] priorityに無効な値を指定した場合に400エラーが返ること
[X] statusに無効な値を指定した場合に400エラーが返ること
[X] due_dateに無効な日付形式を指定した場合に400エラーが返ること
[X] 同一タイトルのToDoがすでに存在する場合（かつそれが更新対象と異なる場合）に409エラーが返ること

### 2.5 共通APIレスポンス検証
[X] エラーレスポンスの形式が一貫していること
[X] APIレスポンスのContent-Typeがapplication/jsonであること
[X] 無効なJSON形式のペイロードに対して適切なエラーが返されること
[X] 許可されていないHTTPメソッドに対して適切なエラー(405)が返されること


## 3. エッジケーステスト
### 3.1 境界値テスト
[X] titleがちょうど100文字の場合に正常に処理されること
[X] descriptionがちょうど500文字の場合に正常に処理されること
[ ] due_dateが現在日付の場合に正常に処理されること
[ ] due_dateが過去の日付の場合に正常に処理されること
[ ] due_dateが遠い将来の日付の場合に正常に処理されること
[ ] due_dateがうるう年の2月29日の場合に正常に処理されること

### 3.2 データ量テスト
[X] 大量のToDoアイテム（例：100件以上）がある場合でもGET /todosが正常に動作すること


## 4. 同時実行・競合テスト
[-] 同一IDのToDoを複数クライアントが同時に更新しようとした場合の整合性が確保されること
-> ユーザごとに分けるため、テスト不要
[ ] 同時に多数のリクエストを受けた場合でも正常に処理されること


## 5. エラーハンドリングテスト
### 5.1 データベースエラー
[ ] データベース接続エラー時に適切な500エラーが返ること
[ ] データベースクエリ実行エラー時に適切な500エラーが返ること
[ ] トランザクション障害時に適切なエラーハンドリングが行われること

### 5.2 その他のサーバーエラー
[ ] 予期しない例外発生時に適切な500エラーが返ること
[ ] メモリ不足などのリソース制約時の挙動が適切であること

### テスト方法
5.1, 5.2いずれも方法が不明瞭なため、要相談


## 6. セキュリティテスト
[ ] SQLインジェクション攻撃への耐性があること
[ ] クエリパラメータや入力値のサニタイズが適切に行われること
[-] 大量リクエストによるDoS攻撃への対策があること
-> DoS対策はデプロイ先のインフラのサービスで対策する。開発環境では特にテストせず、検証環境でテストする。

## 7. 統合テスト
[X] エンドツーエンドのワークフローが正常に機能すること
[X] ToDoの作成→取得→更新→削除の一連の流れ
[X] フィルタリングを含むシナリオ
[X] アプリケーション層とデータベース層の統合が正常に機能すること

### 方法
E2Eテストとして、VSCodeのREST Clientという拡張機能で実施済み

## 8. パフォーマンステスト
[X] レスポンス時間が許容範囲内であること（例：95%のリクエストが200ms未満）
[-] 大量データ処理時のメモリ使用量が適切であること
-> 大人数のユーザ数を想定していないため、テスト不要
